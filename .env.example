# QA Hub configuration (copy to `.env` for local dev)
#
# NOTE:
# - Never commit real credentials.
# - Credentials are read by the Electron main process from `.env` (loaded into process.env).
# - The renderer (Vite) will NOT get access to these unless you prefix with VITE_.

# Azure DevOps
AXIS_AZURE_ORG=
AXIS_AZURE_PROJECT=
AXIS_AZURE_PAT=

# Jira
AXIS_JIRA_BASE_URL=
AXIS_JIRA_EMAIL=
AXIS_JIRA_API_TOKEN=

# BrowserStack (used for both Automate + Test Management)
AXIS_BROWSERSTACK_USERNAME=
AXIS_BROWSERSTACK_ACCESS_KEY=
